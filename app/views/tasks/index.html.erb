Task
<!-- app/views/restaurants/index.html.erb -->
<ul>
  <% @tasks.each do |task| %>
    <li><%= task.completed? ?  "✅" : "⛔️" %><%= link_to task.title, task_path(task) %> <%= link_to "Delete",
            task_path(task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

</br></li>
  <% end %>
</ul>

<%= link_to "Add a new task", new_path %>
